{
    "configurations": [
        {
            "name": "Attach to Expo",
            "request": "attach",
            "type": "reactnativedirect",
            "cwd": "${workspaceFolder}"
        }
    ]
}