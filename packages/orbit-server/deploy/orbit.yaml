apiVersion: apps/v1
kind: Deployment
metadata:
  name: orbit-server
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  replicas: 1
  selector:
    matchLabels:
      app: orbit-server
  template:
    metadata:
      labels:
        app: orbit-server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: orbit
        image: docker.korshakov.com/orbit-server:{version}
        ports:
        - containerPort: 3005
        env:
        - name: NODE_ENV
          value: production
        - name: PORT
          value: "3005"
        - name: REDIS_URL
          value: redis://orbit-redis:6379
        - name: S3_PUBLIC_URL
          value: https://files.cluster-fluster.com/orbit
        envFrom:
        - secretRef:
            name: orbit-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orbit-server-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: orbit-server
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: orbit-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: orbit-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /orbit-db
    - extract:
        key: /orbit-master
    - extract:
        key: /orbit-github
    - extract:
        key: /orbit-files
    - extract:
        key: /orbit-e2b
    - extract:
        key: /orbit-revenuecat
    - extract:
        key: /orbit-elevenlabs
---
apiVersion: v1
kind: Service
metadata:
  name: orbit-server
spec:
  selector:
    app: orbit-server
  ports:
  - port: 3000
    targetPort: 3005
  type: ClusterIP
